# =========================================================================
# external-worker environment. Copy to .env and fill in. .env is git-ignored.
# =========================================================================

DEV_MODE=False
ENABLE_PERFORMANCE_LOGGER=True

# --- Shared PostgreSQL cluster ---
DB_USER=
DB_PASSWORD=
DB_HOST=your-postgres-host
DB_PORT=5432
# Two physical databases after the AIXII consolidation (aviation data + infra):
DB_AIXII_NAME=aixii
DB_SERVICE_NAME=service

# --- Shared Redis (ARQ broker — same instance as core-api) ---
REDIS_USER=
REDIS_USER_PASSWORD=
REDIS_HOST=your-redis-host
REDIS_PORT=6379

# --- Scheduling (ARQ cron). Enable on EXACTLY ONE instance. ---
SCHEDULER_ENABLED=false

# --- Fleet-sheet edits -> forecast report, applied automatically in batches (needs SCHEDULER_ENABLED) ---
# Checked every POLL s; applied once the editing is QUIET s old, or the oldest edit waited MAX_WAIT s.
FLEET_EDITS_AUTO_APPLY=true
FLEET_EDITS_POLL_SECONDS=15
FLEET_EDITS_QUIET_SECONDS=10
FLEET_EDITS_MAX_WAIT_SECONDS=60
# How long a forecast run / restore waits for the staging table before refusing
FORECAST_STAGING_LOCK_WAIT_SECONDS=180

# --- Webhook URL building (kept in sync with core-api) ---
SELF_HOST=api.aixii.com
API_ROOT_URL=/api/v1
# REQUIRED — same value as core-api
MS_WEBHOOK_SECRET=

# --- Microsoft Graph ---
MS_TENANT_ID=
MS_CLIENT_ID=
MS_CLIENT_SECRET=
MS_GRAPHSCOPES=https://graph.microsoft.com/.default

# --- Airlabs ---
AIRLABS_API_KEY=

# --- FlightRadar ---
FLIGHT_RADAR_API_KEY=

# --- Aviation Edge ---
AVIATION_EDGE_API_KEY=
AVIATION_EDGE_EXTRA_API_KEY=
